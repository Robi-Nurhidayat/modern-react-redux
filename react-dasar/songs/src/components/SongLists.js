import React, { Component } from "react";
import { connect } from "react-redux";

class SongLists extends Component {
  render() {
    return <div>Songlist</div>;
  }
}

export default connect()(SongLists);
